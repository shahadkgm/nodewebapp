<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>
    <link rel="stylesheet" href="/css/style.css"> <!-- Add your CSS file -->
</head>
<body>
    <h1>User Profile</h1>

    <!-- User Details -->
    <section>
        <h2>Profile Details</h2>
        <p><strong>Name:</strong> <%= user.name %></p>
        <p><strong>Email:</strong> <%= user.email %></p>
        <p><strong>Phone:</strong> <%= user.phone %></p>
        <button onclick="document.getElementById('edit-profile').style.display='block'">Edit Profile</button>
    </section>

    <!-- Address -->
    <section>
        <h2>Saved Addresses</h2>
        <% if (user.addresses && user.addresses.length > 0) { %>
            <ul>
                <% user.addresses.forEach(address => { %>
                    <li><%= address.street %>, <%= address.city %>, <%= address.state %>, <%= address.zip %></li>
                <% }); %>
            </ul>
        <% } else { %>
            <p>No addresses saved yet.</p>
        <% } %>
    </section>

    <!-- Orders -->
    <section>
        <h2>Order History</h2>
        <% if (orders && orders.length > 0) { %>
            <ul>
                <% orders.forEach(order => { %>
                    <li>
                        <p><strong>Order ID:</strong> <%= order._id %></p>
                        <p><strong>Status:</strong> <%= order.status %></p>
                        <p><strong>Date:</strong> <%= order.createdAt.toDateString() %></p>
                        <% if (order.status !== 'Cancelled') { %>
                            <form method="POST" action="/profile/orders/cancel">
                                <input type="hidden" name="orderId" value="<%= order._id %>">
                                <button type="submit">Cancel Order</button>
                            </form>
                        <% } %>
                    </li>
                <% }); %>
            </ul>
        <% } else { %>
            <p>No orders found.</p>
        <% } %>
    </section>

    <!-- Change Password -->
    <section>
        <h2>Change Password</h2>
        <form method="POST" action="/profile/change-password">
            <label for="currentPassword">Current Password</label>
            <input type="password" name="currentPassword" required>
            
            <label for="newPassword">New Password</label>
            <input type="password" name="newPassword" required>

            <button type="submit">Change Password</button>
        </form>
    </section>

    <!-- Edit Profile Modal -->
    <div id="edit-profile" style="display:none;">
        <h2>Edit Profile</h2>
        <form method="POST" action="/profile/edit">
            <label for="name">Name</label>
            <input type="text" name="name" value="<%= user.name %>" required>
            
            <label for="email">Email</label>
            <input type="email" name="email" value="<%= user.email %>" required>
            
            <label for="phone">Phone</label>
            <input type="text" name="phone" value="<%= user.phone %>" required>

            <button type="submit">Save Changes</button>
        </form>
        <button onclick="document.getElementById('edit-profile').style.display='none'">Cancel</button>
    </div>

</body>
</html>
 -->
